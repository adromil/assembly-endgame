export default function Header() {
  return (
    <header className="header">
      <div className="header__title">
        <span className="header__prompt">&gt;_</span>
        <h1>
          ASSEMBLY<span className="header__colon">:</span>ENDGAME
        </h1>
      </div>
      <p className="header__subtitle">
        Guess the word one letter at a time. Every miss retires a language
        from the call stack. Only Assembly never leaves.
      </p>
    </header>
  );
}
